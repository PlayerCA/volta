# netlify.toml — Volta deployment config

[build]
  publish = "."   # Root folder — no build step needed

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
